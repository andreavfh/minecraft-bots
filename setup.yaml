variables:
  password: "elpepe"
  prueba: "robot"

onJoin:
  - command: "skin set WhiteSamael"
  - say: "I am running forward"
  - move: forward
    duration: 10000
  - delay: 500




responses:
  - if: "welcome"
    say: "i'm a {{prueba}}"
  - if: "are you ok"
    say: yes
  - if: "please move"
    move: left
    duration: 600
  - if: "/register" 
    command: "register {{password}} {{password}}"
  - if: "/login"
    command: "login {{password}}"
  - if: "Acceso exitoso"
    register: true
  - if: "get out"
    quit: true
